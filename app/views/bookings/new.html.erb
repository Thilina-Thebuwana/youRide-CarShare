<% if user_signed_in? %>
      <% if current_user.try(:admin?) %>
        
       
        <%= render 'form', booking: @booking %>

        <text><%= link_to 'Back', bookings_path %></text>
        
        <% else %>

        <%= render 'form', booking: @booking %>

       
        <%end%>
        
<%else%>

<%end%>
